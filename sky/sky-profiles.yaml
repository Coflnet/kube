apiVersion: apps/v1
kind: Deployment
metadata:
  name: player
  namespace: sky
spec:
  selector:
    matchLabels:
      app: player
  template:
    metadata:
      labels:
        app: player
    spec:
      imagePullSecrets:
        - name: harbor
      containers:
      - name: player
        image: harbor.flou.dev/coflnet/skyprofiles:1e67117f-e80a-43fb-8340-3e2831851a57
        ports:
          - containerPort: 8000
        env:
          - name: REDIS_HOST
            value: player-redis
          - name: KAFKA_HOST
            valueFrom:
              configMapKeyRef:
                name: config
                key: kafka_host
          - name: JAEGER_AGENT_HOST
            valueFrom: 
              configMapKeyRef:
                name: config
                key: jaeger_agent_host
        resources:
          limits:
            memory: "512Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: player
  namespace: sky
spec:
  selector:
    app: player
  ports:
  - port: 80
    targetPort: 8000