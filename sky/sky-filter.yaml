apiVersion: apps/v1
kind: Deployment
metadata:
  name: filter
  namespace: sky
  labels:
    app: filter
spec:
  selector:
    matchLabels:
      app: filter
  replicas: 1
  template:
    metadata:
      labels:
        app: filter
    spec:
      imagePullSecrets:
        - name: harbor
      containers:
      - name: filter
        image: harbor.flou.dev/coflnet/skyfilter:5ff492fb-0ead-49c0-8785-bdf70c9b760b
        ports:
        - containerPort: 80
          name: web
        env: 
          - name: JAEGER_AGENT_HOST
            valueFrom: 
              configMapKeyRef:
                name: config
                key: jaeger_agent_host
---
apiVersion: v1
kind: Service
metadata:
  name: filter
  namespace: sky
spec:
  selector:
    app: filter
  ports:
  - port: 80
    targetPort: 80
