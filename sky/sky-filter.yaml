apiVersion: apps/v1
kind: Deployment
metadata:
  name: filter
  namespace: sky
  labels:
    app: filter
spec:
  selector:
    matchLabels:
      app: filter
  replicas: 1
  template:
    metadata:
      labels:
        app: filter
    spec:
      imagePullSecrets:
        - name: harbor
      containers:
      - name: filter
        image: harbor.flou.dev/coflnet/skyfilter:dae94fb0-1d8e-4e99-adba-5e67e6d2bc40
        ports:
        - containerPort: 80
          name: web
        env: 
          - name: JAEGER_AGENT_HOST
            valueFrom: 
              configMapKeyRef:
                name: config
                key: jaeger_agent_host
---
apiVersion: v1
kind: Service
metadata:
  name: filter
  namespace: sky
spec:
  selector:
    app: filter
  ports:
  - port: 80
    targetPort: 80
