apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfl-tip-game-claim
  namespace: nfl-tip-game
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi