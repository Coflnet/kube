apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: discord-bot-node
  namespace: discord
spec:
  selector:
    matchLabels:
      app: discord-bot-node
  serviceName: discord-bot-node
  template:
    metadata:
      labels:
        app: discord-bot-node
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: discord-bot-node
        image: registry.flou.dev/discord-bot-node:ec356403-a544-4b8a-8b75-61bf2d92e8bf
        env:
          - name: TOKEN
            valueFrom:
              secretKeyRef:
                name: secret
                key: token
          - name: CHANNEL_ID_BUGREPORT
            valueFrom:
              configMapKeyRef:
                name: config
                key: live_channel_id_bugreport
          - name: CHANNEL_ID_SUGGESTIONS
            valueFrom:
              configMapKeyRef:
                name: config
                key: live_channel_id_suggestion
          - name: CHANNEL_ID_SUPPORT
            valueFrom:
              configMapKeyRef:
                name: config
                key: live_channel_id_support

        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: discord-bot-node
spec:
  selector:
    app: discord-bot-node
  ports:
  - port: 80
    targetPort: 80
