apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: discord-bot-node-eventsource
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    discord-bot-node:
      port: "12000"
      endpoint: /discord-bot-node
      method: POST