apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: homepage-eventsource
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    homepage:
      port: "12000"
      endpoint: /homepage
      method: POST