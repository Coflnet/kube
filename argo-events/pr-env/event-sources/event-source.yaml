apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: pr-env-eventsource
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    pr-env:
      port: "12000"
      endpoint: /pr-env
      method: POST
