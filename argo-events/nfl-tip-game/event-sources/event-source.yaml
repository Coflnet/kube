apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: nfl-tip-game-eventsource
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    nfl-tip-game:
      port: "12000"
      endpoint: /nfl-tip-game
      method: POST