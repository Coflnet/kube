apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: kafka-sim-eventsource
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  webhook:
    kafka-sim:
      port: "12000"
      endpoint: /kafka-sim
      method: POST