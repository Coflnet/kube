apiVersion: apps/v1
kind: Deployment
metadata:
  name: homepage
  namespace: homepage
  labels:
    app: homepage
spec:
  selector:
    matchLabels:
      app: homepage
  replicas: 1
  template:
    metadata:
      labels:
        app: homepage
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
      - name: homepage
        image: registry.flou.dev/homepage:ccca99cc-4526-4b0b-9724-b7eb3d59b334
        ports:
        - containerPort: 80
          name: web
        resources:
          requests:
            cpu: 2m
            memory: 10Mi
---
apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage
spec:
  selector:
    app: homepage
  ports:
  - port: 80
    targetPort: 80
