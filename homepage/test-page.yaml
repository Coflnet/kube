
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: test-page
  namespace: homepage
spec:
  replicas: 5
  selector:
    matchLabels:
      app: test-page
  template:
    metadata:
      labels:
        app: test-page
    spec:
      imagePullSecrets:
        - name: harbor
      containers:
      - name: test-page
        image: harbor.flou.dev/flou21/test-page:babacbc8-3b93-4154-95ca-2bd9e54aaa13
        resources:
          requests:
            memory: 10Mi
            cpu: 10m
          limits:
            memory: 20Mi
            cpu: 20m
        ports:
        - containerPort: 80
          name: web
  minReadySeconds: 30
  revisionHistoryLimit: 3
  strategy:
    canary: 
      steps:
      - setWeight: 20
      - pause: {duration: 5m}
      - analysis:
          templates:
          - templateName: success-rate
          args:
          - name: service-name
            value: test-page-svc
      maxSurge: "25%"
      maxUnavailable: 1
      canaryService: test-page-canary-svc # required
      stableService: test-page-stable-svc # required
      trafficRouting:
        smi:
          rootService: test-page
          trafficSplitName: test-page-canary-traffic-split
---
apiVersion: v1
kind: Service
metadata:
  name: test-page
  namespace: homepage
spec:
  selector:
    app: test-page
  ports:
  - port: 80
    targetPort: 80
