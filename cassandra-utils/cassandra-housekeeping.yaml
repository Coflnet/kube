apiVersion: batch/v1
kind: CronJob
metadata:
  name: cassandra-housekeeping
  namespace: cassandra-cluster
spec:
  schedule: "*/5 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          imagePullSecrets:
            - name: regcred
          containers:
          - name: housekeeping
            image: registry.flou.dev/cassandra-housekeeping:50fac4ce-90bb-4ccc-8d71-6127bae90093
            imagePullPolicy: IfNotPresent
            env:
              - name: USERNAME
                value: cassandra
              - name: PASSWORD
                value: rPubG9U8if
              - name: KEYSPACE
                value: jaeger_v1
              - name: HOSTS
                value: cassandra-cluster.cassandra-cluster
          restartPolicy: Never
