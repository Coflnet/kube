apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app: provisioner
  name: provisioner
  namespace: payment
spec:
  template:
    spec:
      imagePullSecrets:
        - name: harbor
      containers:
      - name: product-provisioner
        image: harbor.flou.dev/coflnet/productprovisioner:c16df76f-a9c1-4829-91b6-c84c00ee5789
        volumeMounts:
        - mountPath: /app/products.json
          name: config-volume
          subPath: products.json
        env:
          - name: "payment_host"
            value: "payment"
      restartPolicy: Never
      terminationGracePeriodSeconds: 30
      volumes:
      - configMap:
          items:
          - key: products.json
            path: products.json
          name: config
        name: config-volume
